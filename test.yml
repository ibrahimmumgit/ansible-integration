
---
- name: Test ansible on other node
  hosts: all
  gather_facts: false
  become: yes
  tasks:
    - name: Other host tasts
      cmd: whoami
      register: output
      
    - debug:
        msg: "{{ output }}"
      

